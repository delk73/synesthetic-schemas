{
  "task": "Schema Evaluation (Initialization)",
  "version": "draft-v0.1",
  "owner": "Synesthetic Schemas WG",
  "lastReviewed": "2025-09-22",
  "objective": "Create a baseline snapshot of current schema definitions in the repo. Descriptive only, no prescriptive changes. Output is immutable reference for later audits.",
  "constraints": {
    "style": "deterministic, terse, Markdown headings with bullets/tables",
    "rules": [
      "Ground all findings in visible JSON schema files under defs/ and synesthetic_asset.json",
      "List every field exactly as defined in current repo",
      "No recommendations, no prescriptions",
      "No speculation about missing fields or future design",
      "Always write the full evaluation report to meta/output/schema_eval_<DATE>.md",
      "Always write the snapshot to meta/output/schema_eval_<DATE>.json",
      "Also update meta/output/schema_eval_latest.md and meta/output/schema_eval_latest.json as aliases",
      "DATE must be in YYYYMMDD format",
      "Eval is incomplete unless all four files are created or overwritten with non-empty content"
    ],
    "sections": [
      "Summary of repo schemas",
      "Schema inventory table (Schema → Fields Present)",
      "Component inventories (Shader, Tone, Haptic, Modulation, RuleBundle, Control, Examples)",
      "Top-level asset composition (synesthetic_asset.json)"
    ]
  },
  "scope": {
    "files": [
      "synesthetic_asset.json",
      "defs/*.json",
      "examples/*.json"
    ]
  },
  "output": {
    "paths": [
      "meta/output/schema_eval_<DATE>.md",
      "meta/output/schema_eval_<DATE>.json",
      "meta/output/schema_eval_latest.md",
      "meta/output/schema_eval_latest.json"
    ],
    "format": "Markdown + JSON",
    "must_write": true,
    "stdout_optional": true
  },
  "exit_criteria": [
    "meta/output/_old/schema_eval_<DATE>.md is written and non-empty",
    "meta/output/_old/schema_eval_<DATE>.json is written and non-empty",
    "meta/output/schema_eval_latest.md is written and non-empty",
    "meta/output/schema_eval_latest.json is written and non-empty",
    "All schemas listed with their fields exactly as defined",
    "No prescriptive language present",
    "Snapshot can be reused as immutable baseline for later audits"
  ]
}
